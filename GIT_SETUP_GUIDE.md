# Git 提交指南

## 📋 准备工作

### 1. 初始化 Git 仓库

```bash
# 在项目根目录执行
git init
```

### 2. 配置 Git 用户信息（如果还没配置）

```bash
git config --global user.name "你的名字"
git config --global user.email "your.email@example.com"
```

## 🚀 提交到本地仓库

### 1. 添加所有文件到暂存区

```bash
git add .
```

### 2. 创建初始提交

```bash
git commit -m "Initial commit: Vue3 + DevUI 漏洞管理系统"
```

## ☁️ 提交到远程仓库（GitHub/Gitee）

### 方案 A: 使用 GitHub

#### 1. 在 GitHub 创建新仓库
- 访问 https://github.com/new
- 填写仓库名称（例如：`vulnerability-management-system`）
- 选择 Public 或 Private
- **不要**勾选 "Initialize this repository with a README"
- 点击 "Create repository"

#### 2. 关联远程仓库并推送

```bash
# 添加远程仓库
git remote add origin https://github.com/你的用户名/仓库名.git

# 推送到远程仓库
git branch -M main
git push -u origin main
```

### 方案 B: 使用 Gitee（国内访问更快）

#### 1. 在 Gitee 创建新仓库
- 访问 https://gitee.com/projects/new
- 填写仓库名称
- 选择开源或私有
- 点击 "创建"

#### 2. 关联远程仓库并推送

```bash
# 添加远程仓库
git remote add origin https://gitee.com/你的用户名/仓库名.git

# 推送到远程仓库
git branch -M main
git push -u origin main
```

## 📝 后续提交流程

每次修改代码后：

```bash
# 1. 查看修改状态
git status

# 2. 添加修改的文件
git add .

# 3. 提交修改
git commit -m "描述你的修改内容"

# 4. 推送到远程
git push
```

## 🔧 常用 Git 命令

```bash
# 查看提交历史
git log --oneline

# 查看当前分支
git branch

# 创建新分支
git checkout -b feature/新功能名称

# 切换分支
git checkout main

# 合并分支
git merge feature/新功能名称

# 拉取远程最新代码
git pull

# 查看远程仓库信息
git remote -v
```

## ⚠️ 注意事项

1. **敏感信息**：确保 `.gitignore` 文件已正确配置，不要提交敏感信息（密码、密钥等）
2. **node_modules**：已在 `.gitignore` 中排除，不会被提交
3. **提交信息**：使用清晰的中文或英文描述每次提交的内容
4. **分支管理**：建议使用分支进行功能开发，完成后再合并到 main 分支

## 📦 推荐的提交信息格式

```bash
# 功能开发
git commit -m "feat: 添加用户登录功能"

# Bug 修复
git commit -m "fix: 修复登录页面样式问题"

# 文档更新
git commit -m "docs: 更新 README 文档"

# 样式调整
git commit -m "style: 优化数据库页面布局"

# 代码重构
git commit -m "refactor: 重构漏洞列表组件"

# 性能优化
git commit -m "perf: 优化表格渲染性能"
```

## 🎯 快速开始（一键执行）

如果你想快速完成初始化和首次提交，可以依次执行：

```bash
# 初始化并首次提交
git init
git add .
git commit -m "Initial commit: Vue3 + DevUI 漏洞管理系统

项目包含以下功能：
- 用户登录系统
- 仪表板数据展示
- 漏洞数据库管理
- 消息中心
- 漏洞报告生成"

# 关联远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/仓库名.git

# 推送到远程
git branch -M main
git push -u origin main
```

完成后，你的项目就成功提交到 Git 仓库了！🎉
